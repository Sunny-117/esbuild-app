{
  "name": "esbuild-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild app.jsx --bundle --outfile=out.js --minify",
    "buildts": "esbuild main.ts --bundle --outfile=main.js --minify"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.15.10",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
