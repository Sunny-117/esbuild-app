{
  "name": "esbuild-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild app.jsx --bundle --outfile=out.js",
    "buildts": "esbuild main.ts --bundle --outfile=main.js --minify",
    "buildwatch": "esbuild main.ts --bundle --outfile=main.js --watch",
    "testjs": "esbuild app.js --bundle --outfile=out.js --loader:.js=jsx"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.1.2",
    "esbuild": "^0.15.10",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
